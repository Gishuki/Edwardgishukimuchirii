// Function to handle the display of contact info when the "Contact Me" button is clicked
function contactMe() {
  const contactInfo = document.getElementById('contact-info');
  
  // Toggle the "show" class to either show or hide the contact info
  contactInfo.classList.toggle('show');
}
